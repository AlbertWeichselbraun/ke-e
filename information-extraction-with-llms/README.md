# Use case: Information Extraction with LLMs

1. `fetch-wiki-pages.py` retrieves the Wiki pages for all Characters in the `harry-potter.ttl` ontology.

2. `extract_relatives.py` afterwards creates a JSON file that contains all relatives of the retrieved characters.
